## Github 项目地址

本项目Github仓库： [https://github.com/smysong/mediamaster-v2](https://github.com/smysong/mediamaster-v2 "https://github.com/smysong/mediamaster-v2")

## Docker 仓库地址

本项目Docker仓库： [https://hub.docker.com/r/smysong/mediamaster-v2](https://hub.docker.com/r/smysong/mediamaster-v2 "https://hub.docker.com/r/smysong/mediamaster-v2")

本项目Docker镜像原始拉取命令：`docker pull smysong/mediamaster-v2:latest`

本项目Docker镜像代理加速拉取命令`docker pull docker.1ms.run/smysong/mediamaster-v2:latest`

## 管理信息

默认管理信息：

> 默认端口：8888
> 使用浏览器访问：http://your-ip:8888
> 默认用户名：admin
> 默认密码：password


## ⚠️ 部署前特别提示
<div style="color: red;">
**⚠️ 使用transmission或qbittorrent下载器需要在下载器设置中勾选“未完成任务添加后缀”选项，具体设置请查看：[参考文档](http://wiki.songmy.top:8080/web/#/686311457/102215720 "参考文档")**

**⚠️ 如需使用硬链接，请务必参考[媒体库目录结构与文件转移策略说明](http://wiki.songmy.top:8080/web/#/686311457/102215726 "媒体库目录结构与文件转移策略说明")进行媒体库目录结构以及Docker挂载目录路径配置的调整，不可完全按照本部署文档中的volumes路径进行配置。**
</div>

## 程序工作流程
1. 部署完成后登录web管理进行系统设置。
2. 系统设置完成后重启docker容器，程序将根据系统配置获取豆瓣想看数据并加入正在订阅。
3. 订阅添加完成后程序将进行资源搜索，找到可以资源时会进行下载。
4. <div style="color: red;">系统启动时自动执行以上流程，流程结束后，程序以系统设置中“自动化流程间隔”时长进行定时运行，并非实时运行，所以新添加的订阅会在下次运行时进行搜索和下载。也可以在服务控制中手动运行搜索和下载任务。</div>

## 系统最低配置要求
- CPU：≥4核 1.5Ghz
- 内存：≥1GB
- Docker镜像文件磁盘空间：≥2GB

<div style="color: red;">
*注意：使用最低配置运行时请关闭非必要的站点以节省系统资源消耗。否则有导致宿主机宕机的风险！（建议仅开启2个站点）
</div>

## 快速部署

### 飞牛FPK一键部署：
请参考[Mediamaster-v2飞牛第三方FPK安装包一键部署](https://www.songmy.top/blog/9 "Mediamaster-v2飞牛第三方FPK安装包一键部署")

### Docker Compose部署：
请替换volumes路径为实际路径。docker.1ms.run是docker镜像代理拉取地址，可以去除或更改为其他代理地址。

**（单独部署） docker-compose.yml 如下：**
```
version: '3.8'
services:
  mediamaster:
    image: docker.1ms.run/smysong/mediamaster-v2:latest
    container_name: mediamaster
    environment:
      - UID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户ID进行修改
      - GID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户组ID进行修改
      - GIDLIST=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户附加组列表进行修改
      - TZ=Asia/Shanghai #设置中国时区
    volumes:
      - /vol1/1000/Media:/Media #媒体库目录，可根据实际进行修改
      - /vol1/1000/Downloads:/Downloads #下载目录，可根据实际进行修改
      - /vol1/1000/Torrent:/Torrent #可选配置:使用qBittorrent、Transmission、迅雷，无需配置种子监听目录。如使用其他下载器监听种子文件需进行配置
      - /vol1/1000/Docker/mediamaster:/config #可根据实际进行修改
      - /vol1/1000/Data:/Data #如需硬链接，需将媒体库目录和下载目录放置在同一挂载点下，同时删除上方媒体库目录和下载目录的配置，/Data为挂载点可根据实际需求进行名称修改
    ports:
      - 8888:8888
    restart: always
    networks:
      media-network:

networks:
  media-network:
    driver: bridge
    enable_ipv6: true #如本地网络无IPV6可设置为false
    ipam:
      config:
        - subnet: 172.16.238.0/24 #如可自行修改
        - subnet: 2001:db8:1::/64 #如本地网络无IPV6可删除
```

**（包含下载器-Bridge模式）以transmission下载器为例 docker-compose.yml 如下：**
```
version: '3.8'
services:
  mediamaster:
    image: docker.1ms.run/smysong/mediamaster-v2:latest
    container_name: mediamaster
    networks:
      media-network:
    environment:
      - UID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户ID进行修改
      - GID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户组ID进行修改
      - GIDLIST=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户附加组列表进行修改
      - TZ=Asia/Shanghai #设置中国时区
    volumes:
      - /vol1/1000/Media:/Media #媒体库目录，可根据实际进行修改
      - /vol1/1000/Downloads:/Downloads #下载目录，可根据实际进行修改
      - /vol1/1000/Docker/mediamaster:/config #可根据实际进行修改
      - /vol1/1000/Data:/Data #如需硬链接，需将媒体库目录和下载目录放置在同一挂载点下，同时删除上方媒体库目录和下载目录的配置，/Data为挂载点可根据实际需求进行名称修改
    ports:
      - 8888:8888
    restart: always

  transmission:
    image: docker.1ms.run/linuxserver/transmission:latest
    container_name: transmission
    networks:
      media-network:
    environment:
      - PUID=0
      - PGID=0
      - TZ=Asia/Shanghai #设置中国时区
    volumes:
      - /vol1/1000/Docker/transmission:/config #可根据实际进行修改
      - /vol1/1000/Downloads:/downloads #可根据实际进行修改
    ports:
      - 9091:9091
      - 51413:51413
      - 51413:51413/udp
    restart: unless-stopped

networks:
  media-network:
    driver: bridge
    enable_ipv6: true #如本地网络无IPV6可设置为false
    ipam:
      config:
        - subnet: 172.16.238.0/24
        - subnet: 2001:db8:1::/64 #如本地网络无IPV6可删除
```

**（包含下载器-Host模式）请根据实际情况选择合适下载器，以下 docker-compose.yml 包含：transmission、qbittorrent、迅雷**
```
version: '3.8'
services:
  mediamaster:
    image: docker.1ms.run/smysong/mediamaster-v2:latest
    container_name: Mediamaster
    network_mode: host
    environment:
      - UID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户ID进行修改
      - GID=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户组ID进行修改
      - GIDLIST=0 #如遇权限问题导致文件无法创建、修改等，请根据实际用户附加组列表进行修改
      - TZ=Asia/Shanghai #设置中国时区
      - PORT=9999 # 可选配置：自定义设置容器内应用端口，不提供则使用默认8888端口运行
    volumes:
      - /vol1/1000/Media:/Media #媒体库目录，可根据实际进行修改
      - /vol1/1000/Downloads:/Downloads #下载目录，可根据实际进行修改
      - /vol1/1000/Docker/mediamaster:/config #可根据实际进行修改
      - /vol1/1000/Data:/Data #如需硬链接，需将媒体库目录和下载目录放置在同一挂载点下，同时删除上方媒体库目录和下载目录的配置，/Data为挂载点可根据实际需求进行名称修改
    restart: always

  transmission:
    image: docker.1ms.run/linuxserver/transmission:latest
    container_name: Transmission
    network_mode: host
    environment:
      - PUID=0 #同上，遇权限问题可进行修改
      - PGID=0 #同上，遇权限问题可进行修改
      - TZ=Asia/Shanghai #设置中国时区
    volumes:
      - /vol1/1000/Docker/transmission:/config #可根据实际进行修改
      - /vol2/1000/Downloads:/downloads #可根据实际进行修改
    restart: always

  qbittorrent:
    image: docker.1ms.run/linuxserver/qbittorrent:latest
    container_name: Qbittorrent
    network_mode: host
    environment:
      - PUID=0 #同上，遇权限问题可进行修改
      - PGID=0 #同上，遇权限问题可进行修改
      - WEBUI_PORT=7008 #可根据实际进行修改
      - TORRENTING_PORT=6881 #可根据实际进行修改
      - TZ=Asia/Shanghai #设置中国时区
    volumes:
      - /vol1/1000/Docker/qbittorrent:/config #可根据实际进行修改
      - /vol2/1000/Downloads:/downloads #可根据实际进行修改
    restart: always

  xunlei:
    image: cnk3x/xunlei:latest
    privileged: true
    container_name: xunlei
    hostname: Fn_Nas
    network_mode: host
    volumes:
      - /vol1/1000/Docker/xunlei:/xunlei/data #可根据实际进行修改
      - /vol2/1000/Downloads:/xunlei/downloads #可根据实际进行修改
    restart: unless-stopped
```

## 手动部署
### 使用命令行部署：

#### 步骤 1: 拉取镜像

**首先，你需要从 Docker Hub 拉取镜像。在已经安装了 Docker 的机器上：**

```bash
docker pull docker.1ms.run/smysong/mediamaster-v2:latest
```

这将下载 smysong/mediamaster-v2:latest 镜像到本地。

#### 步骤 2: 运行镜像

接下来，你可以运行这个镜像。需要挂载一些配置文件或数据卷，你需要指定适当的挂载点。以下是示例命令，用于运行镜像，并挂载配置文件和数据卷：

Bridge模式：

```
docker run -d \
  --name mediamaster \
  --network=bridge \
  -v /path/to/config:/config \
  -v /Media:/Media \
  -v /Torrent:/Torrent \
  -v /Downloads:/Downloads \
  -p 8888:8888 \
  docker.1ms.run/smysong/mediamaster-v2:latest
```

Host模式：

```
docker run -d \
  --name mediamaster \
  --network=host \
  -v /path/to/config:/config \
  -v /Media:/Media \
  -v /Torrent:/Torrent \
  -v /Downloads:/Downloads \
  -e PORT=9999 \
  docker.1ms.run/smysong/mediamaster-v2:latest
```

在以上命令参数解释：


> -it
> 表示以交互模式运行容器，并分配一个伪TTY。
>
> --name mediamaster \
> 给容器命名。
>
>以下所有-v -p参数，冒号前是主机上的配置，冒号后是容器内的配置
>
> -v /path/to/config:/config \
> 在主机上/path/to/config目录挂载到容器的 /config 目录，用于存放配置文件。
>
> -v /Media:/Media \
> 将主机上的/Media媒体根目录挂载到容器的/Media目录，用于扫描本地媒体库文件和nfo文件。
>
> -v /Torrent:/Torrent \
> 可选配置：在主机上新建Torrent 目录挂载到容器的 /Torrent 目录，用于存放下载的种子文件。
> qBittorrent或Transmission已经支持通过API接口直接添加种子任务，迅雷远程设备通过专用程序监听种子，无需配置种子监听目录。如使用其他下载器监听种子文件时才需配置种子文件映射目录。
>
> -v /Downloads:/Downloads \
> 下载目录，将主机上下载器的下载目录挂载到容器/Downloads目录，可以实现自动转移并重命名下载完成的影片文件。
>
> -p 8888:8888 \
> bridge模式时容器外映射到容器内的端口。
>
> -e PORT=9999 \
> Host模式时指定WEB服务的自定义运行端口，未提供则使用默认8888端口，
>
> docker.1ms.run/smysong/mediamaster-v2:latest \
> 是要运行的镜像名称。

### 使用Docker管理器部署（以飞牛为例）：

#### 拉取镜像

在镜像仓库中搜索：`smysong/mediamaster-v2:latest`或使用手动拉取功能填写镜像地址进行拉取镜像到本地

![拉取镜像1](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=18ea93ee357852f1a4fd6a6c13a09e38 "拉取镜像1")

![拉取镜像2](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=ead4090e04253393cfde0d97174a8c4c "拉取镜像2")

#### 创建容器
1、使用拉取的镜像创建新容器，可根据实际情况设置自动启动或资源限制：

![创建1](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=b59e985c66ae18172f1d3c154bcfda0c "创建1")

2、配置端口映射：

![创建2](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=34cf94e0b59fa7fccebf97170901c608 "创建2")

3、配置存储空间目录映射：

![创建3](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=1065b7652edda45c124ebd2ee87549da "创建3")

4、配置网络模式（默认使用bridge模式，可选host模式，host模式不支持自定义修改端口号）：

![创建4](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=9159dd2ce4ffe8f3c902043bccb8f058 "创建4")

5、其余选项保持默认，确认配置无误后创建容器即可：

![创建5](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=57f64abec9545506efdc6e03dc6e77f3 "创建5")

## 登录WEB管理并进行基础设置

WEB管理信息：
> 默认端口：8888
> 使用浏览器访问：http://your-ip:8888
> 默认用户名：admin
> 默认密码：password

登录后进入系统设置页，进行配置修改，完成后保存配置并重启容器，以下是简单配置及说明：
（详细配置项说明请查看：[系统设置选项说明](http://wiki.songmy.top:8080/web/#/686311457/102215716 "系统设置选项说明")）

![设置1](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=5b4227d5149741be0c02c25932f4cee7 "设置1")

![设置2](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=27ba177b9edff80f7e170bd24770d87e "设置2")

<div style="color: red;">
 **必填项：获取自己的豆瓣账号ID，并填写到豆瓣订阅地址中：**
</div>

通过网页版豆瓣获取：登录豆瓣账号后，进入“个人主页”或“我的豆瓣”在页面中获取豆瓣ID

![设置3](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=cc3872715fc4826ef58124c725013160 "设置3")

在系统设置中替换豆瓣ID：

![设置4](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=ae9c0b3d65fe28a6775804655693c5f8 "设置4")

正确的豆瓣订阅地址在浏览器中打开是能看到类似下图中订阅内容，请进行验证：

![设置5](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=1d297ebc6d2fd601245356b447a38264 "设置5")

豆瓣API密钥和豆瓣Cookie保持系统默认，无需修改。TMDB API密钥需自行申请

![设置6](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=e94bfbfd8b06a6ac023c60e829aee426 "设置6")

配置下载器用户名、密码、IP地址、端口号等信息。（如下载器没有设置用户名和密码，则保持默认值或删除用户名和密码）

![设置7](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=add244abbb21640b247f7d35435e34bf "设置7")

私有（付费注册）站点配置说明： 私有站点支持：[高清剧集网](https://www.bthdtv.com/)、[高清影视之家](https://www.hdbthd.com/)
特别提示：两个私有站点在注册时需保持用户名和密码一致。

![设置8](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=fb3c0a870b5a2c608d4f10d774d0b1ed "设置8")

公开（免费注册）站点配置说明： 公开站点支持：[不太灵影视](https://www.butailing.com/)、[观影](https://www.gyg.si/)、[BT影视](https://www.btbtl.com/ "BT影视")

~~![设置9](http://wiki.songmy.top:8080/server/index.php?s=/api/attachment/visitFile&sign=032a758ab043fab8dc678cc6fa42c91f "设置9")~~

#### 配置下载器监听目录（可选）
- qBittorrent或Transmission已经支持通过API接口直接添加下载任务，无需配置种子监听目录。
- 迅雷远程设备通过程序内置的专用监听程序监听种子文件，无需配置种子监听目录。
- 使用其他下载器需要配置下载器监听主机上的torrent目录，发现新的种子文件后将自动下载。（需支持种子监听功能的下载器：如飞牛下载、极空间下载等）

